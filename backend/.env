# PostgreSQL — used by Prisma at runtime
# Local Postgres occupies 5432-5434, so docker-compose exposes our container on 5435.
DATABASE_URL="postgresql://todo_user:todo123@localhost:5435/tododb?schema=public"
PORT=8000
HOST=0.0.0.0
CORS_ORIGINS="http://localhost:5173,http://localhost:3000,http://localhost"
